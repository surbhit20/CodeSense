[

    {
        "type": "function",
        "function": {
            "name": "retriever",
            "description": "Fetches the full content of a specific file from the codebase using its file path.",
            "parameters": {
                "type": "object",
                "properties": {
                    "filepath": {
                        "type": "string",
                        "description": "The full path to the file that needs to be retrieved."
                    }
                },
                "required": ["filepath"]
            }
        }
    }
    
]